# Generated Results

Run:

```bash
python run_simulation.py
```

to generate the Gillespie-versus-analytical comparison figure.

The PNG output is ignored by Git by default. Remove `results/*.png` from
`.gitignore` if you want to commit the generated figure.
